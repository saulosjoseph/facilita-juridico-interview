import SingleTon from "../../../Infrastructure/singleTon";
import { BaseController } from "../../../infra/http/controllers/baseController";

// export const makeListClientController = (): BaseController => {
//   const useCase = makeListClientUseCase(SingleTon.getInstance().clientRepository);
//   return new CreateClientController(validation, useCase);
// };
